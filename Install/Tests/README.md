# Windows Unified Connector Test Packages

Constains project for msi and exe packages

Run with the followign to fail:
`.\TestPackageBootstrapper.exe -q FAIL=true`

`msiexec.exe /i "TestPackage.msi" FAIL=true`